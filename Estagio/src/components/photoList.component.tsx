import React from 'react';
import PhotoItem from '../components/photoItem.component';

const PhotoList = () => {
  return (
    <>
      {/* FlatList aqui */}
      <PhotoItem />
    </>
  );
};

export default PhotoList;
